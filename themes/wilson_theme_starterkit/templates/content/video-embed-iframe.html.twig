{#
/**
 * @file
 * Display an iframe with alterable components.
 */
#}
<iframe{{ attributes }} title="{{ 'Video player'|t }}"{% if url is not empty %} src="{{ url }}{% if query is not empty %}?{{ query | url_encode }}{% endif %}{% if fragment is not empty %}#{{ fragment }}{% endif %}"{% endif %}></iframe>
