{% set classes = [
  'relative',
  'block',
  'flex',
  'w-full',
  'flex-col',
  link_url ? 'group' : '',
] %}

<div{{ attributes.addClass(classes) }}>
  {{ title_prefix }}
  {{ title_suffix }}

  {% if image %}
    <div class="mb-5 rounded-lg overflow-hidden relative">
      <div class="group-hover:scale-110 transition-transform duration-300">
        {{ image }}
      </div>
    </div>
  {% endif %}

  <div class="pb-8">
    {% if icon %}
      <div class="text-4xl md:text-6xl mb-4">
        {{ icon }}
      </div>
    {% endif %}

    {% if pre_header %}
      <p class="uppercase font-semibold tracking-wider text-xs mt-0 mb-2">{{ pre_header }}</p>
    {% endif %}

    {% if headline %}
      <h3 class="font-medium text-2xl mt-0 mb-4 last:mb-0">{{ headline }}</h3>
    {% endif %}

    {% if caption %}
      <p>{{ caption|render|striptags }}</p>
    {% endif %}
  </div>

  {% if link_url or published_date %}
    <div class="mt-auto pb-5 flex flex-row flex-wrap justify-between gap-3 items-center">
      {% if link_url %}
        <a href="{{ link_url }}" class="btn btn--secondary after:absolute after:inset-0 text-sm">{{ link_text }}</a>
      {% endif %}

      {% if published_date %}
        <time class="font-light text-xs whitespace-nowrap">{{ published_date }}</time>
      {% endif %}
    </div>
  {% endif %}
</div>
